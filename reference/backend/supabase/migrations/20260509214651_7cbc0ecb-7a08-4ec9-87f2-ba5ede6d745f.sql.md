# Source reference: supabase/migrations/20260509214651_7cbc0ecb-7a08-4ec9-87f2-ba5ede6d745f.sql

Sanitized, inert source; not executable or a production schema export.

````text

SELECT cron.schedule(
  'chat-photo-gallery-reminders-hourly',
  '15 * * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/chat-photo-gallery-reminders',
    headers := '{"Content-Type": "application/json", "apikey": "REDACTED_LAB_VALUE"}'::jsonb,
    body := jsonb_build_object('triggered_at', now())
  );
  $$
);

````
