# Source reference: supabase/migrations/20260516224250_db354ded-fa02-4672-8180-878258462b6c.sql

Sanitized, inert source; not executable or a production schema export.

````text
-- Re-enable per-team DM event reminder toggle
UPDATE public.teams SET auto_rsvp_dm_enabled = true;

-- Reschedule the DM event reminder cron (every 15 minutes)
DO $$ BEGIN PERFORM cron.unschedule('auto-rsvp-dm-cron'); EXCEPTION WHEN OTHERS THEN NULL; END $$;

SELECT cron.schedule(
  'auto-rsvp-dm-cron',
  '*/15 * * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/auto-rsvp-dm-cron',
    headers := '{"Content-Type":"application/json","apikey":"REDACTED_LAB_VALUE","Authorization":"Bearer REDACTED_TOKEN"}'::jsonb,
    body := jsonb_build_object('triggered_at', now())
  );
  $$
);
````
