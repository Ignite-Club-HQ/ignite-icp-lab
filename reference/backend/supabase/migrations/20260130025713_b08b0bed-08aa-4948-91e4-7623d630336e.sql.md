# Source reference: supabase/migrations/20260130025713_b08b0bed-08aa-4948-91e4-7623d630336e.sql

Sanitized, inert source; not executable or a production schema export.

````text
-- Create cron job for sending invite reminders daily at 9 AM UTC
SELECT cron.schedule(
  'send-invite-reminders-daily',
  '0 9 * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/send-invite-reminders',
    headers := '{"Content-Type": "application/json", "Authorization": "Bearer REDACTED_LAB_VALUE"}'::jsonb,
    body := '{}'::jsonb
  ) AS request_id;
  $$
);
````
