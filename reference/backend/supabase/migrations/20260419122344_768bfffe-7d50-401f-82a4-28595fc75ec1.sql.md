# Source reference: supabase/migrations/20260419122344_768bfffe-7d50-401f-82a4-28595fc75ec1.sql

Sanitized, inert source; not executable or a production schema export.

````text
-- Schedule the process-scheduled-messages worker to run every minute.
-- Unschedules first to avoid duplicates if re-run.
DO $$
BEGIN
  PERFORM cron.unschedule('process-scheduled-messages-every-minute');
EXCEPTION WHEN OTHERS THEN
  -- Job didn't exist yet; ignore.
  NULL;
END $$;

SELECT cron.schedule(
  'process-scheduled-messages-every-minute',
  '* * * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/process-scheduled-messages',
    headers := '{"Content-Type": "application/json", "Authorization": "Bearer REDACTED_LAB_VALUE"}'::jsonb,
    body := jsonb_build_object('time', now())
  );
  $$
);
````
