# Source reference: supabase/migrations/20260404200332_ae8f6e96-23d4-4812-8d8c-f118c6840fb3.sql

Sanitized, inert source; not executable or a production schema export.

````text

SELECT cron.schedule(
  'send-engagement-reminders-daily',
  '0 10 * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/send-engagement-reminders',
    headers := '{"Content-Type": "application/json", "x-cron-secret": "' || coalesce((SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'CRON_SECRET' LIMIT 1), '') || '"}'::jsonb,
    body := '{}'::jsonb
  ) AS request_id;
  $$
);

````
