# Source reference: supabase/migrations/20260511001411_05c34d7b-b323-4397-9b53-bcecde65290c.sql

Sanitized, inert source; not executable or a production schema export.

````text
CREATE EXTENSION IF NOT EXISTS pg_cron WITH SCHEMA extensions;
CREATE EXTENSION IF NOT EXISTS pg_net WITH SCHEMA extensions;

-- Drop any prior schedule of this job
DO $$
BEGIN
  PERFORM cron.unschedule('auto-default-rsvp-confirm-cron');
EXCEPTION WHEN OTHERS THEN NULL;
END $$;

SELECT cron.schedule(
  'auto-default-rsvp-confirm-cron',
  '*/30 * * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/auto-default-rsvp-confirm-cron',
    headers := '{"Content-Type":"application/json","apikey":"REDACTED_LAB_VALUE"}'::jsonb,
    body := jsonb_build_object('time', now())
  );
  $$
);
````
