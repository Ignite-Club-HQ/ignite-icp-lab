# Source reference: supabase/migrations/20260315055432_77960bc1-8e03-4926-823a-d2dd3551a8fd.sql

Sanitized, inert source; not executable or a production schema export.

````text

SELECT cron.schedule(
  'auto-purge-trash-daily',
  '0 2 * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/auto-purge-trash',
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'Authorization', 'Bearer ' || coalesce((SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'CRON_SECRET' LIMIT 1), '')
    ),
    body := '{"triggered_at": "now"}'::jsonb
  ) AS request_id;
  $$
);

````
