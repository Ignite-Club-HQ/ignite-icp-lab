# Source reference: supabase/migrations/20260125135635_0880ccb6-464f-433a-a932-c545a73952f1.sql

Sanitized, inert source; not executable or a production schema export.

````text
-- Fix the pending invite that was created with wrong invited_user_id
-- This invite for redacted@example.invalid incorrectly has the inviter's ID
UPDATE public.pending_invites
SET invited_user_id = NULL
WHERE invited_email IS NOT NULL
  AND invited_user_id IS NOT NULL
  AND status = 'pending'
  AND invited_user_id = invited_by_user_id;
````
