# Source reference: supabase/migrations/20260215073622_83cca2fc-62da-4222-8b4e-ebc503580e11.sql

Sanitized, inert source; not executable or a production schema export.

````text
-- Change retry-missed-push-notifications cron from every 2 minutes to every 1 minute
-- The pg_net trigger is consistently failing, so this reduces worst-case delay from 2min to 1min
SELECT cron.unschedule(6);

SELECT cron.schedule(
  'retry-missed-push-notifications',
  '* * * * *',
  $$
  SELECT net.http_post(
    url := 'REDACTED_LAB_VALUE/functions/v1/retry-missed-push-notifications',
    headers := '{"Content-Type": "application/json", "Authorization": "Bearer REDACTED_LAB_VALUE"}'::jsonb,
    body := '{}'::jsonb
  ) AS request_id;
  $$
);
````
